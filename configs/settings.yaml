# List of patient IDs
patient:
  - 'p01'
  - 'p02'
  - 'p03'
  - 'p04'
  - 'p05'
  - 'p06'
  - 'p07'
  - 'p08'
  - 'p13'

# Dataset name to use
dataset: "clear"

# Enable or disable debug mode
debug_mode: true


method_list:
  - "xgboost"
  - "fast_ldgd"
  - "ldgd"

metric_list:
  - "accuracy"
  - "f1_score"
  - "precision"
  - "recall"

#  data
features_selection_method : "all"  # Options: all, corr, pre_selected
feature_transformation : "normalize"  # Options: normalize, standardize, None
target_column : "ColorLev"  # Options: decision, old_new, is_go, is_correct, is_experienced


# LDGD Configs
data_dim : None
latent_dim : 3
num_inducing_points : 10
cls_weight : 1
use_gpytorch : True
shared_inducing_points : True

# Training LDGD
load_trained_model : False
batch_size : 100
num_epochs_train : 5000
num_epochs_test : 5000


# Save features to a file
save_features: false

# Load a pretrained model
load_pretrained_model: true

# Number of folds for cross-validation mode (It can be a number of block for pilot1 task)
cross_validation_mode: 5

# Proportion of the dataset to include in the test split
test_size: 0.2

# Load features from a file
load_features: false

# Load epoched data
load_epoched_data: true

# Save epoched data to a file
save_epoched_data: false

# Load preprocessed data from a file
load_preprocessed_data: false

# Save preprocessed data to a file
save_preprocessed_data: false
